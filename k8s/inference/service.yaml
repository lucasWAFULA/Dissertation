apiVersion: v1
kind: Service
metadata:
  name: inference-svc
  namespace: market-pulse
  labels:
    app: inference
spec:
  type: ClusterIP
  selector:
    app: inference
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
